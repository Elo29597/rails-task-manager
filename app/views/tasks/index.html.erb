<h2>MY TASKS</h2>
<% @tasks.each do |task| %>
   <% if task.completed == true %>
   <p><input type=“checkbox” checked=“checked”><%= link_to task.title, task_path(task) %></p>
   <% else %>
   <p><input type=“checkbox”><%= link_to task.title, task_path(task) %></p>
 <% end %>
<% end %>
